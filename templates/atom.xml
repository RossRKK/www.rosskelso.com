{#- Zola's default atom feed, with /drafts/ dropped — see content/drafts/_index.md. -#}
<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}">
    <title>{% block title %}{{ config.title }}{% endblock title %}</title>
    {%- if config.description %}
    <subtitle>{{ config.description }}</subtitle>
    {%- endif %}
    <link rel="self" type="application/atom+xml" href="{{ feed_url | safe }}"/>
    <link rel="alternate" type="text/html" href="{{ config.base_url | safe }}"/>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>{{ last_updated | date(format="%+") }}</updated>
    <id>{{ feed_url | safe }}</id>
    {%- for page in pages %}
    {%- if "/drafts/" not in page.permalink %}
    <entry xml:lang="{{ page.lang }}">
        <title>{{ page.title }}</title>
        <published>{{ page.date | date(format="%+") }}</published>
        <updated>{{ page.updated | default(value=page.date) | date(format="%+") }}</updated>
        <author>
          <name>{{ page.authors | first | default(value=config.author | default(value="Unknown")) }}</name>
        </author>
        <link rel="alternate" type="text/html" href="{{ page.permalink | safe }}"/>
        <id>{{ page.permalink | safe }}</id>
        {%- if page.summary %}
        <summary type="html">{{ page.summary }}</summary>
        {%- else %}
        <content type="html">{{ page.content }}</content>
        {%- endif %}
    </entry>
    {%- endif %}
    {%- endfor %}
</feed>
